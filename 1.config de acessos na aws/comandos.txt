
# configura as suas credenciais na AWS
- aws configure 

# visualizar o bucket que está na AWS via linha de comando
- aws s3 ls nome_do_bucket

# cria um arquivo .txt e grava 'hello world' nele
- echo "Hello world" > file-test.txt

# faz o upload desse arquivo para o bucket na s3
- aws s3 cp file-test.txt s3://nome_do_bucket

# lista o arquivo que foi salvo na s3
- aws s3 ls nome_do_bucket