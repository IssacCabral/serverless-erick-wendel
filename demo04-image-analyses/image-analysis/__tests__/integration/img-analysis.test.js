const { describe, it, expect } = require("@jest/globals");

describe("Image Analyser test suit", () => {
  it.todo(
    "it should analyse successfuly the image returning the results",
    () => {}
  );

  it.todo(
    "given an empty queryString it should return status code 400",
    () => {}
  );

  it.todo("given an invalid ImageURL it should return 500", () => {});
});
